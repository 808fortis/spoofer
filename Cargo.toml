[package]
name = "zygisk-spoof"
version = "1.0.0"
edition = "2021"

[lib]
name = "zygisk"
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
libc = "0.2"

[profile.release]
lto = true
strip = true
opt-level = "z"
panic = "abort"
